grades = [80, 90, 70] 
grades[0]
grades[1]
print(grades[-2]) # will print 90 count from the last index of the lists, the last index of the lists is -1
# Slicing a list
grades[1:2]
80 in grades
60 in grades
len(grades)
min(grades)

subjects = ['bio', 'cs', 'math', 'history']
len(subjets)
max(subjects)
sum(subjects) #return will error
street_address = [10, 'Main Street']


for grade in grades:
    print(grade)


for item in subjects:
    print(item)


# Lists can contain any type of data, including integers, strings, and even other lists.
# A list is usually used to contain items of the same kind, like temperatures or dates or grades in a course.



